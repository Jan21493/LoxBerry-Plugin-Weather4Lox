---
phase: 03-cgi-json-endpoint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - webfrontend/htmlauth/show.cgi
autonomous: false
requirements:
  - CGI-01
  - CGI-02
  - CGI-03
must_haves:
  truths:
    - "Browser-Aufruf von show.cgi?format=json&type=current gibt JSON mit Content-Type: application/json; charset=utf-8 zurueck"
    - "Browser-Aufruf mit type=hourly bzw. type=daily gibt die jeweiligen JSON-Daten zurueck"
    - "HTTP-Response enthaelt Cache-Control: no-cache Header"
    - "Fehlender oder ungueltiger type-Parameter gibt HTTP 400 mit JSON-Error zurueck"
    - "Fehlende JSON-Datei auf Disk gibt HTTP 404 mit JSON-Error zurueck"
    - "Bestehende HTML-Template-Funktionalitaet von show.cgi bleibt unveraendert"
  artifacts:
    - path: "webfrontend/htmlauth/show.cgi"
      provides: "JSON API endpoint block inserted after query parameter parsing"
      contains: "format.*json"
  key_links:
    - from: "webfrontend/htmlauth/show.cgi (JSON block)"
      to: "$home/log/plugins/$psubfolder/current.json"
      via: "open(<:raw) file slurp"
      pattern: "open.*:raw.*json_path"
    - from: "webfrontend/htmlauth/show.cgi (JSON block)"
      to: "$home/log/plugins/$psubfolder/hourlyforecast.json"
      via: "type_map hash lookup"
      pattern: "hourly.*hourlyforecast\\.json"
    - from: "webfrontend/htmlauth/show.cgi (JSON block)"
      to: "$home/log/plugins/$psubfolder/dailyforecast.json"
      via: "type_map hash lookup"
      pattern: "daily.*dailyforecast\\.json"
---

<objective>
show.cgi um einen JSON-Endpoint erweitern, der Wetterdaten als JSON ueber HTTP bereitstellt.

Purpose: Browser-Clients (ocean-live Theme in Phase 4/5) koennen current, hourly und daily Wetterdaten per Fetch API laden, ohne HTML zu parsen.
Output: show.cgi mit funktionierendem JSON-Endpoint (format=json&type=current|hourly|daily)
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cgi-json-endpoint/03-CONTEXT.md
@.planning/phases/03-cgi-json-endpoint/03-RESEARCH.md

@webfrontend/htmlauth/show.cgi

<interfaces>
<!-- Key patterns from show.cgi the executor needs -->

From webfrontend/htmlauth/show.cgi (lines 58-76) — query parameter parsing:
```perl
foreach (split(/&/,$ENV{'QUERY_STRING'})){
  ($namef,$value) = split(/=/,$_,2);
  # ... URL decode ...
  $query{$namef} = $value;
}
```
Result: `%query` hash contains all URL parameters. Use `$query{format}` and `$query{type}`.

From webfrontend/htmlauth/show.cgi — established patterns:
```perl
# File path pattern (line 149):
"$home/log/plugins/$psubfolder/dailyforecast.dat"

# Early-exit pattern (lines 122-139):
if ($map) { print "Content-type: text/html\n\n"; ... exit; }

# Header output pattern (line 396):
print "Content-type: text/html\n\n";
```

JSON file names from Phase 1 schema:
- current.json
- hourlyforecast.json
- dailyforecast.json
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert JSON API endpoint block into show.cgi</name>
  <files>webfrontend/htmlauth/show.cgi</files>
  <action>
Insert a new code block into show.cgi at approximately line 77 — AFTER the query parameter parsing loop (line 76) and BEFORE the "If it is not set, use defaults from config" section (line 78). This block handles all JSON API requests and exits before any HTML logic runs.

The block must:

1. Check if `$query{format}` equals `'json'`. If not, fall through to existing HTML logic (no change to existing behavior).

2. Validate `$query{type}` against a whitelist hash:
   ```perl
   my %type_map = (
       'current' => 'current.json',
       'hourly'  => 'hourlyforecast.json',
       'daily'   => 'dailyforecast.json',
   );
   ```
   If type is missing or invalid: return HTTP 400 with JSON error body `{"error": "Invalid or missing type parameter. Use type=current|hourly|daily", "code": 400}`.

3. Build the file path using LoxBerry variables: `"$home/log/plugins/$psubfolder/$type_map{$type}"`

4. Check file existence with `-e`. If not found: return HTTP 404 with JSON error body `{"error": "Weather data not available", "code": 404}`.

5. Open with `<:raw` layer (no encoding mangling). If open fails: return HTTP 500 with JSON error body `{"error": "Internal server error", "code": 500}`.

6. Slurp file content using `local $/` in a scoped block, print to STDOUT, exit.

7. ALL responses (success and error) must include these headers:
   - `Status: NNN Reason\n` (for non-200 only — omit for 200 success)
   - `Content-type: application/json; charset=utf-8\n`
   - `Cache-Control: no-cache\n`
   - `\n` (blank line separator before body)

8. Every response branch must end with `exit;` to prevent fall-through to HTML logic.

Use the complete code example from 03-RESEARCH.md as the reference implementation. Add a section comment header `# JSON API ENDPOINT` with hash-line separators matching the existing style (e.g., lines 18-19, 31-32).

IMPORTANT: Do NOT modify any existing code. Do NOT use CGI.pm's header() function (show.cgi prints raw headers). Do NOT parse/transform the JSON content — raw file slurp + print.
  </action>
  <verify>
    <automated>cd "//192.168.10.100/Media/~~=APPZ=~~/~~=CLAUDE_PPROJEKTE=~~/Loxberry Weather4Lox" && perl -c webfrontend/htmlauth/show.cgi 2>&1 || true</automated>
  </verify>
  <done>
- show.cgi contains a JSON API block after line 76 that handles format=json requests
- Block includes type_map for current/hourly/daily mapping to correct JSON filenames
- Three error paths (400, 404, 500) return proper Status headers and JSON error bodies
- Success path reads file with :raw layer, slurps content, prints with correct headers
- All responses include Content-type: application/json; charset=utf-8 and Cache-Control: no-cache
- Every branch ends with exit
- perl -c syntax check passes (note: may warn about missing LoxBerry modules in dev environment — that is expected)
- Existing HTML template functionality is completely untouched
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify JSON endpoint on live LoxBerry</name>
  <what-built>JSON API endpoint in show.cgi that serves weather data as JSON via HTTP. Supports format=json with type=current|hourly|daily parameters. Returns proper Content-Type, Cache-Control headers, and structured error responses (400/404/500).</what-built>
  <how-to-verify>
On a running LoxBerry instance with Weather4Lox plugin and JSON files present on RAM disk:

1. Test successful current data response:
   ```
   curl -v "http://LOXBERRY_IP/plugins/weather4lox/show.cgi?format=json&type=current"
   ```
   Expected: HTTP 200, Content-Type: application/json; charset=utf-8, Cache-Control: no-cache, valid JSON body with meta/data structure

2. Test hourly and daily:
   ```
   curl -v "http://LOXBERRY_IP/plugins/weather4lox/show.cgi?format=json&type=hourly"
   curl -v "http://LOXBERRY_IP/plugins/weather4lox/show.cgi?format=json&type=daily"
   ```
   Expected: Same headers, respective forecast data

3. Test error - missing type parameter:
   ```
   curl -v "http://LOXBERRY_IP/plugins/weather4lox/show.cgi?format=json"
   ```
   Expected: HTTP 400, JSON error body with "Invalid or missing type parameter"

4. Test error - invalid type:
   ```
   curl -v "http://LOXBERRY_IP/plugins/weather4lox/show.cgi?format=json&type=invalid"
   ```
   Expected: HTTP 400, same error message

5. Test existing HTML functionality is unbroken:
   ```
   curl -v "http://LOXBERRY_IP/plugins/weather4lox/show.cgi"
   ```
   Expected: Normal HTML page (Content-Type: text/html), no JSON output
  </how-to-verify>
  <resume-signal>Type "approved" if all curl tests pass, or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
- `perl -c webfrontend/htmlauth/show.cgi` passes syntax check
- JSON block is inserted at correct position (after query parsing, before HTML defaults)
- All three type values (current, hourly, daily) map to correct file names
- Error responses use correct HTTP status codes (400, 404, 500)
- No existing code lines are modified or removed
- Manual curl tests on live LoxBerry confirm working endpoint (checkpoint)
</verification>

<success_criteria>
- show.cgi?format=json&type=current returns JSON weather data with correct headers
- show.cgi?format=json&type=hourly returns hourly forecast JSON
- show.cgi?format=json&type=daily returns daily forecast JSON
- Error cases return appropriate HTTP status codes with JSON error bodies
- Existing HTML template rendering is completely unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/03-cgi-json-endpoint/03-01-SUMMARY.md`
</output>
