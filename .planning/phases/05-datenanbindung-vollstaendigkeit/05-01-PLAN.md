---
phase: 05-datenanbindung-vollstaendigkeit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - webfrontend/html/lang-de.json
  - webfrontend/html/lang-en.json
  - webfrontend/html/lang-es.json
  - webfrontend/html/lang-nl.json
  - webfrontend/html/lang-sk.json
  - webfrontend/html/ocean-live.html
autonomous: true
requirements:
  - LANG-01
  - LANG-02
  - DATA-05
  - DATA-06
  - EXTRA-01

must_haves:
  truths:
    - "All 5 lang files contain theme.tab_today, theme.tab_tomorrow, theme.tab_days, theme.gusts, theme.loading, theme.error_loading keys"
    - "ocean-live.html has loading overlay and error banner HTML elements"
    - "ocean-live.html has DOM IDs heroSunrise and heroSunset on the astro-row values"
    - "All hardcoded German labels in ocean-live.html have data-i18n attributes"
  artifacts:
    - path: "webfrontend/html/lang-de.json"
      provides: "German theme labels including tab names, loading, error messages"
      contains: "tab_today"
    - path: "webfrontend/html/lang-en.json"
      provides: "English theme labels"
      contains: "tab_today"
    - path: "webfrontend/html/ocean-live.html"
      provides: "DOM infrastructure for live data binding"
      contains: "loadingOverlay"
  key_links:
    - from: "webfrontend/html/ocean-live.html"
      to: "lang-*.json"
      via: "data-i18n attributes matching theme.* keys"
      pattern: "data-i18n=\"theme\\."
---

<objective>
Prepare ocean-live.html and language files for live data binding.

Purpose: Before AJAX data loading can work (Plan 02), the HTML needs DOM IDs for dynamic elements, data-i18n attributes for i18n, loading/error UI elements, and the lang files need all required keys. This is the "Wave 0" prep that Plan 02 builds on.
Output: Extended lang-*.json files, ocean-live.html with loading overlay, error banner, data-i18n attributes, and DOM IDs for sunrise/sunset.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-datenanbindung-vollstaendigkeit/05-RESEARCH.md

<interfaces>
<!-- Existing theme keys in lang-de.json (theme section, lines 188-251): -->
theme.current_weather, theme.forecast, theme.daily_forecast, theme.hourly_forecast,
theme.temperature, theme.humidity, theme.wind, theme.pressure,
theme.sunrise, theme.sunset, theme.feels_like, theme.precipitation,
theme.cloud_cover, theme.uv_index, theme.visibility, theme.dew_point,
theme.weekdays (array), theme.weekdays_short (array), theme.months (array), theme.months_short (array)

<!-- NEW keys to add (not yet in any lang file): -->
theme.tab_today, theme.tab_tomorrow, theme.tab_days,
theme.hours_today, theme.hours_tomorrow, theme.forecast_7day,
theme.click_hour_detail, theme.loading, theme.error_loading,
theme.gusts, theme.live_dashboard

<!-- Existing DOM IDs in ocean-live.html that need data-i18n or live binding: -->
heroTemp, heroDesc, heroWind, heroIcon, heroHumidity,
hourStripHeute, hourStripMorgen, dailyWrap,
hdrTime, hdrDate

<!-- DOM IDs that do NOT yet exist but must be added: -->
heroSunrise, heroSunset (on astro-row .astro-val elements, lines ~397 and ~412)
loadingOverlay, errorBanner, errorMsg

<!-- Hardcoded German labels that need data-i18n attributes: -->
- Tab buttons: "Heute", "Morgen", "Tage" (line 359-361)
- "Heutige Stunden" / "Morgige Stunden" strip titles (lines 464, 472)
- "7-Tage Vorhersage" heading (line 480)
- "Live Dashboard" in header (line 349)
- Stat labels: "Luftfeuchte", "Wind", "Luftdruck", "Sichtweite" (lines 431-446)
- Extra labels: "Taupunkt", "Niederschlag", "UV-Index", "Bewoelkung", "Boen", "Gefuehlt" (lines 452-457)
- Astro labels: "Sonnenaufgang", "Sonnenuntergang", "Luftfeuchte", "Gefuehlt" (lines 397, 412, 417, 424)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend all 5 lang-*.json with new theme keys</name>
  <files>webfrontend/html/lang-de.json, webfrontend/html/lang-en.json, webfrontend/html/lang-es.json, webfrontend/html/lang-nl.json, webfrontend/html/lang-sk.json</files>
  <action>
Add the following keys to the `theme` object in ALL 5 lang files. Insert them AFTER the existing `dew_point` key and BEFORE `weekdays`. Preserve existing keys exactly.

New keys per language:

**DE (lang-de.json):**
```json
"tab_today": "Heute",
"tab_tomorrow": "Morgen",
"tab_days": "Tage",
"hours_today": "Heutige Stunden",
"hours_tomorrow": "Morgige Stunden",
"forecast_7day": "7-Tage Vorhersage",
"click_hour_detail": "Stunde anklicken fuer Details",
"loading": "Wetterdaten werden geladen...",
"error_loading": "Daten konnten nicht geladen werden.",
"gusts": "Boen",
"live_dashboard": "Live Dashboard"
```

**EN (lang-en.json):**
```json
"tab_today": "Today",
"tab_tomorrow": "Tomorrow",
"tab_days": "Days",
"hours_today": "Today's hours",
"hours_tomorrow": "Tomorrow's hours",
"forecast_7day": "7-day forecast",
"click_hour_detail": "Click hour for details",
"loading": "Loading weather data...",
"error_loading": "Could not load data.",
"gusts": "Gusts",
"live_dashboard": "Live Dashboard"
```

**ES (lang-es.json):**
```json
"tab_today": "Hoy",
"tab_tomorrow": "Manana",
"tab_days": "Dias",
"hours_today": "Horas de hoy",
"hours_tomorrow": "Horas de manana",
"forecast_7day": "Pronostico de 7 dias",
"click_hour_detail": "Clic en hora para detalles",
"loading": "Cargando datos meteorologicos...",
"error_loading": "No se pudieron cargar los datos.",
"gusts": "Rafagas",
"live_dashboard": "Panel en vivo"
```

**NL (lang-nl.json):**
```json
"tab_today": "Vandaag",
"tab_tomorrow": "Morgen",
"tab_days": "Dagen",
"hours_today": "Uren vandaag",
"hours_tomorrow": "Uren morgen",
"forecast_7day": "7-daagse voorspelling",
"click_hour_detail": "Klik op uur voor details",
"loading": "Weergegevens worden geladen...",
"error_loading": "Gegevens konden niet worden geladen.",
"gusts": "Windstoten",
"live_dashboard": "Live Dashboard"
```

**SK (lang-sk.json):**
```json
"tab_today": "Dnes",
"tab_tomorrow": "Zajtra",
"tab_days": "Dni",
"hours_today": "Hodinova predpoved dnes",
"hours_tomorrow": "Hodinova predpoved zajtra",
"forecast_7day": "7-dnova predpoved",
"click_hour_detail": "Kliknite na hodinu pre detaily",
"loading": "Nacitavam udaje o pocasi...",
"error_loading": "Udaje sa nepodarilo nacitat.",
"gusts": "Narazovy vietor",
"live_dashboard": "Zive informacie"
```

Read each file first, then write back with new keys inserted. Ensure valid JSON (no trailing commas). Keep UTF-8 encoding.
  </action>
  <verify>
    <automated>cd "//192.168.10.100/Media/~~=APPZ=~~/~~=CLAUDE_PPROJEKTE=~~/Loxberry Weather4Lox" && for f in webfrontend/html/lang-{de,en,es,nl,sk}.json; do python3 -c "import json; d=json.load(open('$f')); assert 'tab_today' in d['theme'], '$f missing tab_today'; assert 'error_loading' in d['theme'], '$f missing error_loading'; print('OK: $f')" 2>/dev/null || node -e "var d=JSON.parse(require('fs').readFileSync('$f','utf8')); if(!d.theme.tab_today||!d.theme.error_loading) process.exit(1); console.log('OK: $f')"; done</automated>
  </verify>
  <done>All 5 lang-*.json files contain the 11 new theme keys. Existing keys unchanged. JSON is valid.</done>
</task>

<task type="auto">
  <name>Task 2: Add DOM infrastructure to ocean-live.html (loading overlay, error banner, data-i18n, IDs)</name>
  <files>webfrontend/html/ocean-live.html</files>
  <action>
Make the following changes to ocean-live.html. Read the file first.

**A) Add CSS (inside existing `<style>` block, before the closing `</style>`):**

```css
/* ===== LOADING OVERLAY ===== */
.loading-overlay {
  position: fixed; inset: 0; background: rgba(15,23,42,0.6);
  display: flex; align-items: center; justify-content: center;
  z-index: 100; backdrop-filter: blur(4px); -webkit-backdrop-filter: blur(4px);
}
html.light .loading-overlay { background: rgba(240,244,248,0.6); }
.loading-spinner {
  width: 48px; height: 48px; border-radius: 50%;
  border: 3px solid rgba(56,189,248,0.3);
  border-top-color: var(--sky);
  animation: spin 0.8s linear infinite;
}
@keyframes spin { to { transform: rotate(360deg); } }
.error-banner {
  position: fixed; bottom: 20px; left: 50%; transform: translateX(-50%);
  background: rgba(244,63,94,0.15); border: 1px solid var(--rose);
  color: var(--text-primary); padding: 12px 24px; border-radius: 12px;
  z-index: 101; backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px);
  font-size: 0.9rem; max-width: 90%;
}
```

**B) Add loading overlay and error banner HTML (just before `</body>`, before the last `<script>` tag):**

```html
<div id="loadingOverlay" class="loading-overlay" style="display:none">
  <div class="loading-spinner"></div>
</div>
<div id="errorBanner" class="error-banner" style="display:none">
  <span id="errorMsg"></span>
</div>
```

**C) Add DOM IDs for sunrise/sunset on astro-row values:**

Find the two `.astro-val.mono` elements inside the astro-row that show "06:23" and "18:05" (hardcoded sunrise/sunset). Add `id="heroSunrise"` and `id="heroSunset"` respectively:

Before: `<div class="astro-val mono">06:23</div>` (line ~397)
After: `<div class="astro-val mono" id="heroSunrise">06:23</div>`

Before: `<div class="astro-val mono">18:05</div>` (line ~412)
After: `<div class="astro-val mono" id="heroSunset">18:05</div>`

**D) Add data-i18n attributes to all hardcoded German text labels:**

Tab buttons (line ~359-361):
- `<button class="tab active" data-tab="heute">Heute</button>` -> add `data-i18n="theme.tab_today"` attribute
- `<button class="tab" data-tab="morgen">Morgen</button>` -> add `data-i18n="theme.tab_tomorrow"`
- `<button class="tab" data-tab="tage">Tage</button>` -> add `data-i18n="theme.tab_days"`

Header "Live Dashboard" span (line ~349):
- `<span>Live Dashboard</span>` -> `<span data-i18n="theme.live_dashboard">Live Dashboard</span>`

Hour strip titles (lines ~464, ~472):
- `Heutige Stunden &mdash; Stunde anklicken fuer Details` -> wrap in span: `<span data-i18n="theme.hours_today">Heutige Stunden</span> &mdash; <span data-i18n="theme.click_hour_detail">Stunde anklicken fuer Details</span>`
- Same for Morgen: `<span data-i18n="theme.hours_tomorrow">Morgige Stunden</span> &mdash; <span data-i18n="theme.click_hour_detail">Stunde anklicken fuer Details</span>`

Daily heading (line ~480):
- `<h2>7-Tage Vorhersage</h2>` -> `<h2 data-i18n="theme.forecast_7day">7-Tage Vorhersage</h2>`

Stat labels in side-stats (lines ~431-446). For each `.stat-label`:
- "Luftfeuchte" -> `data-i18n="theme.humidity"`
- "Wind" -> `data-i18n="theme.wind"`
- "Luftdruck" -> `data-i18n="theme.pressure"`
- "Sichtweite" -> `data-i18n="theme.visibility"`

Extra stats labels (lines ~452-457). For each `.stat-label`:
- "Taupunkt" -> `data-i18n="theme.dew_point"`
- "Niederschlag" -> `data-i18n="theme.precipitation"`
- "UV-Index" -> `data-i18n="theme.uv_index"`
- "Bewoelkung" -> `data-i18n="theme.cloud_cover"`
- "Boen" -> `data-i18n="theme.gusts"`
- "Gefuehlt" -> `data-i18n="theme.feels_like"`

Astro-row labels (lines ~397, 412, 417, 424):
- "Sonnenaufgang" -> `data-i18n="theme.sunrise"`
- "Sonnenuntergang" -> `data-i18n="theme.sunset"`
- "Luftfeuchte" (3rd astro item) -> `data-i18n="theme.humidity"`
- "Gefuehlt" (4th astro item) -> `data-i18n="theme.feels_like"`

**E) Add DOM IDs to stat values that will be updated by live data (side-stats and extras):**
Add IDs to each `.stat-val` element so Plan 02 can bind live data:
- Side stats: `id="statHumidity"`, `id="statWind"`, `id="statPressure"`, `id="statVisibility"`
- Side stat sub for wind direction: `id="statWindDir"` on the `.stat-sub` with "NW"
- Extra stats: `id="statDewpoint"`, `id="statPrecip"`, `id="statUVI"`, `id="statClouds"`, `id="statGusts"`, `id="statFeelslike"`
- Astro felt-like value: `id="heroFeelslike"` on the 4th astro item's `.astro-val.mono`

Keep all existing text as fallback content inside elements. The German text serves as default until lang JSON is loaded.
  </action>
  <verify>
    <automated>cd "//192.168.10.100/Media/~~=APPZ=~~/~~=CLAUDE_PPROJEKTE=~~/Loxberry Weather4Lox" && grep -c 'data-i18n=' webfrontend/html/ocean-live.html && grep -c 'loadingOverlay' webfrontend/html/ocean-live.html && grep -c 'heroSunrise' webfrontend/html/ocean-live.html && grep -c 'heroSunset' webfrontend/html/ocean-live.html && grep -c 'errorBanner' webfrontend/html/ocean-live.html</automated>
  </verify>
  <done>ocean-live.html has: (1) loading overlay + error banner HTML/CSS, (2) heroSunrise/heroSunset DOM IDs, (3) data-i18n attributes on all hardcoded German labels (at least 18 occurrences), (4) DOM IDs on all stat-val elements for live data binding. File renders correctly in browser with German fallback text.</done>
</task>

</tasks>

<verification>
- All 5 lang files parse as valid JSON
- grep for "data-i18n" in ocean-live.html returns 18+ matches
- grep for "loadingOverlay", "errorBanner", "heroSunrise", "heroSunset" each returns >= 1 match
- ocean-live.html opens in browser and looks identical to Phase 4 (no visual regression -- same German text as fallback)
</verification>

<success_criteria>
- Lang files extended with all 11 new keys each
- ocean-live.html has loading/error UI infrastructure
- All German labels have data-i18n attributes for i18n in Plan 02
- Stat values have DOM IDs for live data binding in Plan 02
- No visual regression when opened in browser
</success_criteria>

<output>
After completion, create `.planning/phases/05-datenanbindung-vollstaendigkeit/05-01-SUMMARY.md`
</output>
