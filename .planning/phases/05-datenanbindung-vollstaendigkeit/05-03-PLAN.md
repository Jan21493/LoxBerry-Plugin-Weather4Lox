---
phase: 05-datenanbindung-vollstaendigkeit
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified: []
autonomous: false
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - DATA-06
  - LANG-01
  - LANG-02
  - LANG-03
  - EXTRA-01

must_haves:
  truths:
    - "Theme loads and displays real weather data from show.cgi"
    - "All UI labels appear in configured language"
    - "Auto-refresh works without page reload"
    - "Loading spinner visible during data fetch"
    - "Error banner appears when data cannot be loaded"
    - "Sunrise/sunset show real times in hero area"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete ocean-live theme with live data, i18n, and auto-refresh.

Purpose: Confirm all 5 Phase 5 success criteria are met before marking the phase complete. This is the final gate for the v1 milestone.
Output: Verified working production-ready theme.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-datenanbindung-vollstaendigkeit/05-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete ocean-live theme with live AJAX data loading, 5-language i18n, auto-refresh, loading/error handling, and sunrise/sunset display.</what-built>
  <how-to-verify>
    1. Open ocean-live.html in browser (from LoxBerry webserver or local file server)
    2. **DATA-01**: Check browser Network tab -- should see 5 fetch requests (current, hourly, daily, lang-de.json, icon_mapping.json)
    3. **DATA-05**: On initial load, a loading spinner should briefly appear
    4. **DATA-06**: Change CGI_BASE in source to invalid URL, reload -- error banner should appear at bottom
    5. **LANG-01 + LANG-02**: Open with ?lang=en -- all labels should switch to English (Today/Tomorrow/Days tabs, stat labels, etc.)
    6. **LANG-02**: Test all 5 languages: ?lang=de, ?lang=en, ?lang=es, ?lang=nl, ?lang=sk
    7. **LANG-03**: Confirm ?lang=XX URL parameter works, default is German
    8. **DATA-02**: Verify tab labels, stat labels, astro labels come from lang file (not hardcoded)
    9. **DATA-03**: Verify weather icons display correctly (check Network tab for icon_mapping.json request)
    10. **DATA-04**: Wait 5+ minutes -- data should refresh (check console or Network tab for new requests)
    11. **EXTRA-01**: Verify sunrise and sunset times in hero astro-row show real values (not "06:23" / "18:05" mock)
    12. Click hour cards -- hero should update with that hour's data
    13. Switch tabs (Heute/Morgen/Tage) -- all should render correctly
    14. Toggle dark/light theme -- all elements should remain visible
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues found.</resume-signal>
</task>

</tasks>

<verification>
All 5 Phase 5 success criteria from ROADMAP:
1. Theme loads weather data automatically on open
2. All UI texts in configured language (DE/EN/ES/NL/SK)
3. Loading indicator visible; error message on failure
4. Auto-refresh every 5+ minutes without reload
5. Sunrise/sunset visible in hero area
</verification>

<success_criteria>
User confirms "approved" -- all Phase 5 success criteria met. Theme is production-ready.
</success_criteria>

<output>
After completion, create `.planning/phases/05-datenanbindung-vollstaendigkeit/05-03-SUMMARY.md`
</output>
