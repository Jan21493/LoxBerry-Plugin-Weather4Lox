---
phase: 02-delivery-layer-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/datatoloxone.pl
autonomous: true
requirements:
  - JSON-07
  - JSON-08
must_haves:
  truths:
    - "datatoloxone.pl loads current.json, dailyforecast.json, hourlyforecast.json at startup"
    - "Script exits with LOGCRIT if any JSON file is missing or malformed"
    - "Current conditions UDP/MQTT values use JSON hash fields instead of positional array access"
    - "Daily forecast UDP/MQTT values use JSON array entries instead of positional array access"
    - "Hourly forecast UDP/MQTT values use JSON array entries instead of positional array access"
    - "calc+N aggregation values use defined() checks instead of -9999 comparisons"
    - "No open() calls for .dat files remain in the script"
  artifacts:
    - path: "bin/datatoloxone.pl"
      provides: "Complete JSON-based delivery layer"
      contains: "load_json_file"
  key_links:
    - from: "bin/datatoloxone.pl"
      to: "current.json, dailyforecast.json, hourlyforecast.json"
      via: "load_json_file at script startup"
      pattern: "load_json_file.*json"
    - from: "bin/datatoloxone.pl JSON data structures"
      to: "UDP/MQTT send calls"
      via: "cur hashref and dfc/hfc array entries"
      pattern: "cur->|dfc_entry->|hfc_entry->"
---

<objective>
Migrate datatoloxone.pl from .dat pipe-delimited file reading to JSON reading for ALL sections: UDP/MQTT current conditions, daily forecast, hourly forecast, calc+N aggregation, HTML template variable population, and emulator index.txt generation.

Purpose: Completes Phase 2 -- the delivery layer reads structured JSON instead of positional .dat fields, eliminating fragile index-based coupling while producing identical UDP/MQTT output for Loxone.
Output: Fully migrated bin/datatoloxone.pl with zero .dat read references remaining.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-delivery-layer-migration/02-CONTEXT.md
@.planning/phases/02-delivery-layer-migration/02-RESEARCH.md
@data/json-schema.md
@bin/datatoloxone.pl

<interfaces>
<!-- The executor MUST read the full 02-RESEARCH.md -- it contains complete field mapping tables
     (.dat position -> JSON field name) for current, daily forecast, and hourly forecast.
     Those tables ARE the migration specification. -->

JSON envelope structure (from json-schema.md):
- current.json: { meta: {...}, data: { field: value, ... } }  -- data is a hashref
- dailyforecast.json: { meta: {...}, data: [ { period: N, field: value, ... }, ... ] }  -- data is array of hashrefs
- hourlyforecast.json: { meta: {...}, data: [ { period: N, field: value, ... }, ... ] }  -- data is array of hashrefs

Key data structures after loading:
- $cur = $cur_json->{data}      -- hashref (current conditions)
- @dfc = @{ $dfc_json->{data} } -- array of hashrefs (daily forecast, ordered by period)
- @hfc = @{ $hfc_json->{data} } -- array of hashrefs (hourly forecast, ordered by period)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JSON loading infrastructure and migrate all datatoloxone.pl sections from .dat to JSON</name>
  <files>bin/datatoloxone.pl</files>
  <action>
This task modifies the ENTIRE datatoloxone.pl in one pass. The research document (02-RESEARCH.md) contains complete field mapping tables -- use them as the authoritative reference for every substitution.

**Step 1: Add JSON infrastructure at top of script**

After the existing `use` statements (~line 31), add:
```perl
use JSON::PP ();
```

Add the `load_json_file` subroutine (per Research Pattern 1) and the `_jval` null-safe helper (per Research code example). Place these before the main program section.

**Step 2: Replace .dat file reads with JSON loading**

Replace the three .dat file open/read/close/split blocks with centralized JSON loading. The three .dat reads are at approximately:
- current.dat: lines 118-124
- dailyforecast.dat: lines 372-379 (look for `open(F,"<$lbplogdir/dailyforecast.dat")`)
- hourlyforecast.dat: lines 608-615 (look for `open(F,"<$lbplogdir/hourlyforecast.dat")`)

Replace with JSON loads at the position of the FIRST .dat read (before current conditions processing):
```perl
my $cur_json = load_json_file("$lbplogdir/current.json");
my $dfc_json = load_json_file("$lbplogdir/dailyforecast.json");
my $hfc_json = load_json_file("$lbplogdir/hourlyforecast.json");
my $cur = $cur_json->{data};
my @dfc = @{ $dfc_json->{data} };
my @hfc = @{ $hfc_json->{data} };
```

Remove the other two .dat open/read/close/split blocks entirely.

**Step 3: Migrate current conditions section (~lines 118-361)**

Replace every `@fields[N]` reference with the corresponding `$cur->{field_name}` per the Research "Pattern 2" mapping table. Key points:
- Derived date fields: `cur_date_des` = `$cur->{datetime}`, `cur_date_tz_des` = `$cur->{timezone}`, derive `cur_date_tz_des_sh` and `cur_date_tz` from DateTime (per Research "Deriving Legacy Timezone Fields" code example)
- Sunrise/sunset: use `split(':', $cur->{sunrise})` pattern (Research Pattern 3)
- Unit conversions ($metric): keep identical logic, just swap data source
- Use `_jval()` for fields that could be null/undef where Loxone expects -9999

**Step 4: Migrate daily forecast section (~lines 372-597)**

Replace the `foreach (@dfcdata)` loop with `foreach my $dfc_entry (@dfc)` per Research Pattern 4. Key points:
- Period filtering: `$dfc_entry->{period}` replaces `@fields[0]`
- All field access: `$dfc_entry->{field_name}` per Research DFC mapping table
- Derived date fields (day, month, weekday etc.): use `DateTime->from_epoch(epoch => $dfc_entry->{epoch})` per Research "Derived Date Fields" code example
- Sunrise/sunset: same split(':') pattern as current, but use `$epochdate` as base date (preserve existing behavior per Research Pitfall 4)

**Step 5: Migrate hourly forecast section (~lines 608-783)**

Replace the `foreach (@hfcdata)` loop with `foreach my $hfc_entry (@hfc)`. All field access per Research HFC mapping table.

**Step 6: Migrate calc+N aggregation (~lines 882-1224)**

Replace all `@fields[N]` references in the calc+4, calc+8, etc. loops with `$hfc_entry->{field_name}`. Critical: replace `-9999` sentinel checks with `defined()` checks per Research Pattern 6. This includes:
- `$tmpprec4`, `$tmpprec8` precipitation aggregation
- `@tmpttmean4`, `@tmpttmean8` temperature mean arrays
- `$tmpttmax4`, `$tmpttmin4` etc. min/max tracking
- All conditional checks: `if @fields[N] ne "-9999"` becomes `if defined($hfc_entry->{field})`

**Step 7: Migrate HTML template variable sections (~lines 1298-1553)**

Three template population sections need migration:
- DFC template variables (~lines 1298-1359): `${dfc.$per._tt_h} = @fields[11]` becomes `${dfc.$per._tt_h} = $dfc_entry->{high_temp}` -- variable NAMES stay identical, only data source changes (Research Pattern 8)
- HFC template variables (~lines 1388-1445): same pattern with hourly fields
- Current template variables (~lines 1477-1553): uses `$cur` hashref

For DFC/HFC template sections, also replace the foreach loop over pipe-split arrays with foreach over JSON arrays, matching the same pattern as the UDP/MQTT sections.

**Step 8: Migrate emulator index.txt section (~lines 1693-1822)**

Replace `@fields[N]` references in the emulator section. Key change: `$hfcdate` construction uses `DateTime->from_epoch(epoch => $hfc_entry->{epoch})` instead of constructing from individual year/month/day fields (Research Pattern 7). The emulator re-reads `$curdata` via split -- eliminate this and use `$cur` hashref directly.

**Step 9: Remove ALL remaining .dat references**

Search entire file for: `.dat` in open/read contexts, `@fields[`, `split(/\|/)`, `$curdata`, `@dfcdata`, `@hfcdata`. ALL must be removed or replaced. Zero occurrences must remain (per CONTEXT.md decision: "Alte .dat-Lese-Logik wird komplett entfernt (nicht auskommentiert)").

**Step 10: Keep weatherdata.html debug output**

Per Claude's discretion: retain the weatherdata.html debug file. The `send` subroutine already writes to it -- the $name/$value variables it logs will now contain JSON-sourced values. No changes needed to the send subroutine itself.
  </action>
  <verify>
    <automated>cd "//192.168.10.100/Media/~~=APPZ=~~/~~=CLAUDE_PPROJEKTE=~~/Loxberry Weather4Lox" &amp;&amp; perl -c bin/datatoloxone.pl 2>&amp;1 || true</automated>
  </verify>
  <done>
    - bin/datatoloxone.pl compiles without syntax errors (perl -c)
    - use JSON::PP is present at top of file
    - load_json_file subroutine exists with fail-fast LOGCRIT + exit on missing/malformed JSON
    - _jval null-safe helper exists
    - Three JSON files loaded at script startup: current.json, dailyforecast.json, hourlyforecast.json
    - Zero occurrences of: open() for .dat files, @fields[ references, split pipe delimiter, $curdata/@dfcdata/@hfcdata
    - All UDP/MQTT value names unchanged (cur_*, dfc*_*, hfc*_*, calc+*)
    - All template variable names unchanged (dfc/hfc/cur template vars)
    - Derived date fields preserved: cur_date_des, cur_date_tz_des_sh, cur_date_tz_des, cur_date_tz
    - defined() checks used instead of -9999 comparisons in calc+N aggregation
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify zero .dat remnants and validate field coverage</name>
  <files>bin/datatoloxone.pl</files>
  <action>
Run a comprehensive validation pass on the migrated datatoloxone.pl:

1. **Zero .dat remnants check:** Search the entire file for patterns that indicate incomplete migration:
   - `.dat` in any open() or file path context (not in comments about the migration)
   - `@fields[` anywhere
   - `split(/\|/)` anywhere
   - Variable names: `$curdata`, `@dfcdata`, `@hfcdata`
   If ANY are found, fix them.

2. **JSON field coverage check:** Verify that the key UDP/MQTT send names are present:
   - `cur_tt`, `cur_hu`, `cur_w_sp`, `cur_pr`, `cur_sun_r`, `cur_sun_s` (current)
   - `dfc.*_tt_h`, `dfc.*_tt_l`, `dfc.*_pop`, `dfc.*_sun_r` (daily forecast)
   - `hfc.*_tt`, `hfc.*_hu`, `hfc.*_w_sp` (hourly forecast)
   - `calc+4_prec`, `calc+8_ttmax` (aggregation)

3. **Syntax validation:** Run `perl -c bin/datatoloxone.pl` and fix any issues.

4. **JSON::PP import check:** Verify `use JSON::PP` is present.

5. **load_json_file check:** Verify the function uses LOGCRIT (not LOGERR) for missing files, and flock(LOCK_SH) for shared read locking.

If any issues are found, fix them in this task.
  </action>
  <verify>
    <automated>cd "//192.168.10.100/Media/~~=APPZ=~~/~~=CLAUDE_PPROJEKTE=~~/Loxberry Weather4Lox" &amp;&amp; perl -c bin/datatoloxone.pl 2>&amp;1 &amp;&amp; echo "---REMNANT CHECK---" &amp;&amp; grep -c "fields\[" bin/datatoloxone.pl || echo "0 fields references found"</automated>
  </verify>
  <done>
    - perl -c passes with no errors
    - Zero .dat file open/read calls remain (weatherdata.html is NOT a .dat file -- that stays)
    - Zero @fields references remain
    - Zero split pipe delimiter references remain
    - All UDP/MQTT send names are present and use JSON-sourced values
    - load_json_file uses LOGCRIT and flock
    - JSON::PP is imported
  </done>
</task>

</tasks>

<verification>
1. `perl -c bin/datatoloxone.pl` -- syntax check passes
2. `grep -c "fields\[" bin/datatoloxone.pl` -- returns 0
3. `grep -c 'split.*|' bin/datatoloxone.pl` -- returns 0 (no pipe-split)
4. `grep "\.dat" bin/datatoloxone.pl | grep "open"` -- returns nothing
5. `grep -c "load_json_file" bin/datatoloxone.pl` -- returns 4+ (function def + 3 calls)
6. `grep -c "JSON::PP" bin/datatoloxone.pl` -- returns 1+
</verification>

<success_criteria>
- datatoloxone.pl reads ONLY from current.json, dailyforecast.json, hourlyforecast.json (JSON-07)
- All UDP/MQTT value names and calculation logic are preserved identically (JSON-08)
- Script fails fast with LOGCRIT when JSON files are missing or malformed
- Zero .dat read remnants in the codebase
- HTML template variable names unchanged -- old themes continue working
- perl -c syntax check passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-delivery-layer-migration/02-01-SUMMARY.md`
</output>
